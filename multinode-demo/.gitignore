bin/

# local docker env — generated by local-compute/deploy-docker.sh
local-compute/conf/
local-compute/logs/
local-compute/client/
zpr-conf/admin/multinode-demo.zplc
zpr-conf/admin/multinode-demo.bin2

# OpenTofu / Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Committed on purpose: .terraform.lock.hcl, and terraform.tfvars (holds only
# OCIDs — identifiers, not secrets; real auth lives in ~/.oci/config).
